# IgnittoMedia Workspace Rules

## SEO Analysis
The `seo` skill is available in `.agents/skills/seo/SKILL.md`.
It bridges the installed claude-seo v2.2.0 plugin (located at `C:\Users\Muhammad Umer Farooq\.claude\skills\`) for use with Antigravity.

When the user says `/seo audit`, `/seo page`, `/seo technical`, `/seo content`, `/seo schema`, or any other `/seo` command, trigger the `seo` skill and execute the full workflow described in the SKILL.md.
